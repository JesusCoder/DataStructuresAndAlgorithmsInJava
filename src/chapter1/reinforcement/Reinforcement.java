package chapter1.reinforcement;

public class Reinforcement {
	/**
	 * R1.1 Answer: After the clone, A[4] and B[4] are both pointing to the same
	 * GameEntry object.
	 */
	
	/**R1.2 
	 * Too easy to do it. Pass.*/
}
